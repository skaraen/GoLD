func foo(x int, y int) int {
    var sum int; 
    if (x >= y) {
        return 0; 
    }
    sum = 0; 
    for (x < y) {
        sum = sum + 1;
        x = x + 1;   
    }
    return sum;
}

func main() {
    var a, b int;
    scan a;
    scan b;
    foo(6, 8);
}